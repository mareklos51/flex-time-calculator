# Pliki instrukcji dla agentów AI — lokalne, nie trafiają do repo
# (AGENTS.md to lustrzana kopia CLAUDE.md dla Codeksa)
CLAUDE.md
AGENTS.md

# Notatki robocze — lokalne, nie trafiają do repo
functionality-suggestions.md

# Zrzuty HTML strony UKG (timesheet/vacation) — zawierają dane osobowe, nie trafiają do repo
# Ignorujemy wszystkie .html PRÓCZ popup.html (część rozszerzenia)
*.html
!popup.html

# Archiwum ZIP — generowane lokalnie
*.zip

# Systemy operacyjne
.DS_Store
Thumbs.db

# Edytory
.vscode/
*.swp
*~
